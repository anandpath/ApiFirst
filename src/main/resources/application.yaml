spring:
  application:
    name: 'sample'
server:
  port: 8080
  servlet:
    context-path: '/sample'

# swagger-ui config
springdoc:
  swagger-ui:
    operationSorter: alpha
    displayRequestDuration: true